## The name of the file is the objective

## Key component
- Embedding Model, process to convert text into mathematical vector representation.
- Vector Database: Stores those vector and performs "semantic search".
- LLM (Inference): Receive relevant snippets from database to talk / answer the user.
  


### What we will use:
- Ollama
- Unsloth.ai

We are about to install using either uv or x. Somehow i think it's good to isolate our environment for preparing this data (called pre-traning data)for our AI model. As a good design, this process and the output later, should only have limited access. Explicit access which are given, we dont want this to mess up our life. It seems unnecessary right now, but when we bring openclaw for example, it become matters. 


### State of the art?

The model is changeable, we can experiment using different model. 




#### Referece ? no re-read those, so you can understand better

https://www.ibm.com/think/topics/llm-inference
